# Day3

## What I Did
- for文とif文を組み合わせた処理の練習
- 論理演算子の優先順位の確認
- PEP8を意識してコード整形

##　What I Learned
if文では条件が上から順に評価されること、 elifでは前の条件がFalseの場合のみ評価されることを改めて整理できた。また、for文ではイテラブルを1つずつ処理している構造を理解した。
PEP8というルールを今日の学習の最中に実践とともに理解した。

## Reflection
思考や学習内容を記録として残したかったので今日からこのファイルにまとめていくことにした。
day3.pyの内容は使い方や意味合いも含め全てを自分の言葉で説明できる状態になるまで調べたので、かなり深く理解できた。（特にandとorの優先順位と全体の処理の順番、演算子についてなど）
またPEP8を意識して「見やすいコード」を書くことを今日の学習の最後から実践し始めたので、明日以降は学びと並行してその点も継続して気を付けていきたい。

# Day4

今日はdefで関数を作り、greet()とprint(greet())の違いを学んだ。
まだその先の値の概念やreturnについては理解しきれてないが、予約語や関数の中に関数を入れた時の処理などは深く理解することができた。
PEP8の意識は既に馴染んできているので、無意識にできるところまで慣れていきたい。

# Day5

returnとprintの違いについて学習
Noneの挙動を理解
関数の実行とオブジェクトの違いを整理

# Day6

今日は今までに引き続きPythonの関数の理解を深めた。
昨日学んだreturnとprintの違い、値の扱いへの理解を強固にしつつ主に引数について新しく学習した。
昨日は「値を表示」と「値を返す」の違いの完全な理解に少し手間取ったが、引数に関しては特に引っかかる点もなくスムーズに理解できた。

# Day7

ほんの少しの復習ではあるが学習とpushの習慣を継続できた。

# Day8

Pythonの条件分岐の学習と復習。ifとelseを使って条件によって処理を分ける基本を理解した。

# Day9

ここまでの学習は主にChatGPTを用いながら完全に理解するまで徹底的に独学で調べ実践する方式で学習を進めてきたが、今日からは京都大学の「プログラミング演習2023.pdf」を用いて改めて基礎から一通り学び始めた。
本日の進捗は上記の1∼3章

学習内容：
-変数への値の代入
-四則演算の確認
-データ型の理解：
 -int, float, str, bool
-型変換関数の練習
-複数の変数への代入

感想：
1,2章は導入や紹介、背景などの座学が多かったので実践部分は3章だったが改めて基礎を学べた。
また基礎といっても元々独学でやっていたので新しい学びも多くしっかりと理解するまで反復した。
デバックのようなことも初めてできたのは大きな一歩

# Day10

今日は『京都大学 プログラミング演習2023』第四章の内容を学習しました。
今回の章では、主にニュートン法を用いた平方根の計算を扱いました。

・1ブロックごとに近似値を更新する。
・その値を用いて次の近似値を計算する。
・結果を表示する。

この処理を任意の回数繰り返し、値が徐々に正しい平方根に近づいていく様子を確認できました。
また京都大学の教材にはまだ出てきてはいませんが、常にわからないところや気になったところは都度ChatGPTを用いて理解できるまで調べるようにしているので、その過程でこの任意の繰り返しにはwhile文が使えることも追加で学びました。

その一連の操作の中で「トレース」やエラーメッセージの読み取り方についても学び、以下のような確認をしました。
・変数名をわざと間違えてエラーを発生させる。
・割り算を含む変数に0を代入したときや0の平方根を求めた場合の挙動の確認。

このようなエラーの確認や対処方法、学んだことを生かして「じゃあこのようにコードを書いたらこうなるかな？」というトライをすることは実務を想定してこれからも継続していきたい。

# Day11

今日は『京都大学 プログラミング演習2023』第五章の途中まで学習した。
リストとは複数の値をまとめて扱えるデータ構造であり、a[0]のようにインデックスを指定することで要素にアクセスできることを確認した。

また、range()を使ったリストの生成や、list("abcde")のように文字列からリストを作る方法、split()を使って文章を空白ごとに区切り単語のリストにする方法を学んだ。

学習の中でsplitのスペルをspritとしてしまいエラーが起きたが、エラーメッセージを読みしっかりと対処することができた。

# Day12

五章のさわりと毎日の継続を重視して学習した。
リストの要素へのアクセス、具体的にはリストの長さの表示や負の添え字を用いる方法を理解した。

# Day13

今日は引き続きプログラミング演習2023の五章の学習であるPythonの「リスト」と「オブジェクト」について学んだ。
また最後には演習として、以前書いたニュートン法のコードに、近似した値をまとめるリストと近似した値を出すために用いる二つの値のリストを追記した。

学習内容：
-リストのスライス
-リストの追加と結合:
 -append, extend
-リストの代入と複製
-オブジェクトのミュータブル・イミュータブル
-リストの浅いコピー、深いコピーとその理解
-計算の過程をリストに残す:
 -以前のニュートン法のコードを参照して二種類のリストに1ブロック毎に要素を追加していく

感想：
上記の学習の中でリスト・オブジェクト・メソッドなどそれぞれの単語や概念に対して、自分の中で完全に正しい理解ができるまで調べたのでかなり身についたと思う。
特に浅いコピー・深いコピーなどのコピーや代入それぞれの捉え方の違いについては少し時間がかかってしまったが、参照されているオブジェクト、今回の場合はリストを宝として、オブジェクトを参照している変数を宝のありかを自動的に表示する地図として例えることで正確な理解により早くたどり着く工夫をした。
またエラーなども含め全て確認しながら丁寧に進めているため、最後の演習においてリストのインデックス3の要素の表記が「4.24779963958954445e-06」となっているのを発見し、「エラーは出てないが本来の値とは異なる」「これはエラーではないバグなのか、もしくはなにか自分がコードをまちがえているのか」と考え調べることができた。
ちなみに知らない概念だったのでとても学びにはなったが、その表記は見やすさを意識した値×10のマイナス6という書き方だった。

# Day14

前日までの続きから五章の終わりまで

学習内容：
-`http://www.pythontutor.com`で昨日のコードの動作確認
-タプルと辞書
-リスト、タプル、辞書の分類まとめ

感想：
コードの動作確認では自分の認識があっていることを資格でも再確認できた。
タプルという概念はずっと使っていたものに後出しで名前が付いただけなので簡単だったが、リストとタプルに比べて辞書は少し型が違ったので最初は戸惑った。

# Day15

六章の途中まで

学習内容：
-for文とrangeの併用について
-for ~ in rangeにprintを組み込む・組み込まないの区別
-break, continueの使用

感想：
この教材に移る前にforとrangeは少し触れていたので理解はかなりスムーズにできたと思う。
また前回同様ニュートン法のコードをベースにして付け加えていく学習方法は個人的に肌に合っていてわかりやすかった。
明日はもう少し進度も早めていきたい。

# Day16

本日は、Pythonの反復処理と計算処理を中心に学習した。まず、sum()を使ってrange()の合計を求める方法を確認し、forとrangeを二段階で動かす処理の書き方を練習した。
また、len()をrange()に設定してリストの要素を順番に処理する方法も理解した。

さらに、enumerate()を使うことで、インデックスと要素を同時に取得できる書き方を学習した。
加えて、平均値（average）の計算方法を確認し、リストの要素を処理する際の基本的な計算の流れを整理した。

後半では、リストの内包表記について学び、for文を使う方法よりも簡潔にリストを生成できる書き方を理解した。

最後に、平方根の計算について学習し、精度を指定して計算する方法と、条件を満たすまで繰り返す無限ループ型の計算方法の両方を実装した。
反復処理を利用して数値計算の精度を高める基本的な考え方を確認することができた。

#　Day17

Day17では、while文の使い方を中心に学習した。

昨日は、あらかじめ差分（diff）を計算しておき、その値が一定の精度（1.0e-6）より大きい間だけループを続ける形で平方根を近似計算するコードを書いたが、今日は別の書き方として while True を使った無限ループ型の実装を学んだ。この方法では条件をwhileの部分に書くのではなく、ループ内で差分を計算し十分に小さくなったところで break によってループを終了させることを理解できたので、条件式によってループを制御する方法と、無限ループ＋breakで制御する方法の2つのパターンを理解できた。

また、if文の基本構造（if / elif / else）や条件式の書き方についても教材を読みながら確認した。比較演算子の使い方や文字列同士の比較、括弧を使った演算の優先順位の調整など、条件分岐を書くうえで必要になる基礎的な要素を整理して学習した。

今日実際に書いたコードは平方根を求める無限ループ型のwhile文の実装だが、その周辺知識として条件分岐や比較の考え方をまとめて理解することができた。

# Day18

if文の表示の仕方二種を書き、一つのif内に内包して書くことができると学んだ。
sysはまだexitしかやってないので、明日以降の学習でしっかり理解していきたい。

# Day19

今日も第一に学習の継続を考えてとりあえず触ることができた。
しかしこの二日間は比較的学習に集中できていないので、明日以降は大きく時間をとれるようにしたい。

# Day20

今日は、while True を使った無限ループと try / except を組み合わせた入力処理について学習した。
input()で受け取った値を数値に変換する際にエラーが発生する可能性があるため例外処理を使ってエラーを検知し、continueによって再度入力を促すことで、正しい値が入力されるまで処理を繰り返すプログラムの書き方を理解した。
参考にしている教材の説明から、入力の検証を行う基本的なパターンとしてよく使われる構造であることも理解できた。

また、数値を表示する際のフォーマット指定についても学んだ。
print()で数値を表示する場合、Pythonが自動で桁数や表示形式を決めるが、format()を使うことで表示方法を自分で指定できることを知った。
特に {0:12.8g} のような書式では、表示幅や有効数字、表示形式を指定できる仕組みになっていることを理解した。
他のアルファベットでの働きもざっくり学んだが、今回のようにgを使うと数値の大きさに応じて通常表記と指数表記を自動で切り替えることも確認した。

前の二日間よりはしっかりと学習できたが、休日として考えると自分的にはもっと進めたいので明日以降も頑張りたい。

# Day21

今日は「京都大学Pythonプログラミング演習2023」の第7章の冒頭から途中までを学習した。
テーマは、京都の交差点を題材にしたリストとfor文の組み合わせであり、特に入れ子のfor文と2次元リストの扱いについて理解を深めた。

まず、東西（三条、四条、五条）と南北（堀川、烏丸、河原町）という2つのリストを用意し、for文を入れ子にすることで、それぞれの組み合わせ（交差点）を生成する処理を学んだ。
外側のループで東西、内側のループで南北を回すことで、すべての組み合わせを取り出せることを確認した。

次に、リストの中にリストが入っている「2次元リスト」の基本操作について学習した。
print(a)で全体を表示し、a[0]で1つ目のリスト、a[0][1]でその中の特定の要素にアクセスできることを理解した。
このインデックスの指定方法は今後も頻繁に使うと学んだため、重要な基礎として覚えた。

さらに、2次元リスト内のすべての要素を合計する処理も実装した。
for文を二重に用いてインデックスでアクセスする方法と、for row in a / for element in row のように直接要素を取り出す方法の両方を試し、それぞれの書き方の違いと意味を理解した。
特に、二重ループの動きとして「外側が1回回るごとに内側がすべて実行される」という挙動を意識できたのは大きな収穫だった。

今回の学習を通して、for文には「値を使うためのループ」と「構造を作るためのループ」があることを理解できた点が特に重要だった。
これにより、単なる繰り返し処理だけでなく、データ構造そのものをプログラムで生成するという考え方が身についてきたと感じる。

全体として、2次元リストと入れ子のfor文の基本がかなりクリアになった。
今後は、この構造に対してどのように値を入れたり加工したりするかをさらに練習していきたい。

# Day22

交差点の表を2次元リストとして表現し、各要素に値を代入する処理も行った。
最初は空のリストを手書きで用意していたが、それでは拡張性が低いため、for文を使って動的に表（2次元リスト）を生成する方法を学んだ。
具体的には、外側のループで行を作り、内側のループで列の要素を追加することで、任意のサイズの表を自動で構築できるようになった。

# Day23

今日はいつもの教材の七章続きから七章最後までを学習した。
といってもほとんど読み進めて理解を深めていく形式のものだったので、今回実際書いたのはこの要求を満たすコードを書けといった最後の演習の内容だけだった。
はじめて今までの知識や学習を参考に自力で書けという演習だったが学習記録を参考に自力で書くこと、その後AIを用いて確認をしより良い形にブラッシュアップすること、そしてその道中でいくつかのエラーを確認しエラーメッセージから解決方法を読み取ることで改善することをしっかり経験できた。
都度理解は自分の中で完璧にできるまで学んでいるので組み立ては問題なくできたが、最適化という面ではまだまだだったのでそこも志は高く追及していきたい。

# Day24

今日は教材の第8章「力試し課題」の頭から途中までに取り組み、これまで学習してきた内容をもとに演習形式で自分でコードを書く練習を行った。
主な内容は、平方根を求めるプログラムのブラッシュアップと、ライプニッツの公式を用いた円周率の近似値を計算するプログラムの作成の2つであった。

これまでの学習では教材のコードを模写したり、内容を理解しながら試したりすることが中心だったが、今回のように演習としてゼロからコードを書く形は初めてで、教材やこれまでの知識を参考にしながら自分で構造を考えて書く作業は新鮮で、実際にやってみることで学べることが多いと感じた。

特に、処理の流れや「この処理にはこの機能を使う」といった発想自体は思い浮かぶものの、実際にコードとして書く段階になると、細かい書き方や構文が本当に正しいか自信が持てない場面もあり、知識として理解していることと、実際にコードを書くことの間には差があることを実感した。
だからこそ実際に書いてみることでその差を埋めていく経験ができたのは大きな学びだった。

また、思いついた書き方でまず実装し、その後に「もっと簡潔に書く方法はないか」「より読みやすい書き方はないか」を自分で調べたり、AIを活用して確認したりすることで、コードを改善していく流れも体験できた。
こうした試行錯誤を通して学んでいく方法は、学習効率の面でも非常に有効だと感じた。

平方根の計算プログラムについては、これまで学習してきた内容を組み合わせる形で比較的スムーズに構築できたが、ライプニッツの公式を用いた円周率の近似計算では、自分で処理の形を考えて組み立てる必要があり、より試行錯誤する場面が多かった。
最初に書いたコードをもとに、計算回数を増やして結果を確認したり、改善できる部分を探したりしながら、最終的には見やすさや処理の分かりやすさも意識した形に修正することができた。

また、一度書いたコードで想定通りの結果が出ない場面もあり、その際にはエラーメッセージや処理の流れを確認しながら原因を分解して考え、どの部分の書き方が問題なのかを特定して修正するという、いわゆるデバッグの過程も経験することができた。

今回の演習を通して、コードを書く、見やすさを意識する、エラーを分析して修正する、といった開発の基本的なプロセスを一通り体験できたと感じた。
また、これまで理解を優先して丁寧に学習を進めてきたことで、自分なりのアイデアや解決方法が思い浮かぶ場面も多く、それが正しい方向に近いことも確認できたのは、自信にもつながった。

初めての演習形式だったが、実際に手を動かして考えることで得られる学びが非常に多く、楽しさも感じられる学習になった。

# Day25

昨日に引き続き、今日も教材の第8章の演習問題に取り組んだ。

内容としては、教材で提示された仕様をもとに、トランプのカードデッキを生成しシャッフルする処理を自力でコードとして書くという課題で、リストを用意し、二重のfor文を使ってカードの組み合わせを生成する処理を実装しました。

全体的な発想や処理の流れについては比較的スムーズに考えることができ、必要なコードも自力で書くことができた。

その後、復習としてAIを用いて演習内容について調べ直し、自分の書いたコードと他の書き方を比較しました。

その結果、想定していたよりもシンプルで綺麗に書けている部分もあった一方で、間違いではないものの、より端的でPythonらしい書き方がある部分にも気づくことができた。

そこで本文のコードの後に、それぞれ異なる書き方の例も実際に書き加え、同じ処理を別の方法で表現する練習も行い、それぞれの部分においてよりシンプルに書けるパターンと、少し面倒な方法にはなってしまうものの別の考え方で書くパターンの両方を確認することで、理解を深めることができたと感じています。

今回の演習を通して、コードを書く際には自分が思いついた方法だけでなく、より読みやすく整理された書き方が存在すること、また特定の処理に適した関数の使い方があることを改めて実感しました。

開発系の道に進むうえでも、こうした書き方の違いや選択肢を知ることは実務的に意味のある学びになると感じました。

# Day26

今日はほんとに時間やメンタル面で全く学習の時間がとれなさそうだったので何とか更新するだけになってしまった。

# Day27

昨日に引き続き、生活のことや等ここ数日今後も忙しくて全く学習の時間が取れないが、そういう時にも継続を切らさないことや学習の時間をとるためにその他のやるべきことをしっかりやりきることなど出来ることをしっかりやっていきたい。

# Day28

今日は関数を使って絶対値を求める処理を実装した。入力した数値に対して、条件分岐で負の値を正の値に変換し、結果を出力する流れを理解した。

# Day29

昨日の続き

# Day30

ここ数日熱を出して本当に体調が悪いので学習は全くできていないが継続だけでも続けている。

# Day31

まだ体調は悪いが今日は教材の続き、九章の学習に入った。
今日学んだ部分はその基本のさわりだったが、そのカプセル化という概念はかなり重要だと認識しているので丁寧に理解を深めていきたい。

# Day32

本日は、ニュートン法を用いた平方根の計算処理を実装した。
反復処理を使って近似値を更新し、誤差が一定以下になったら終了する仕組みを理解した。
またユーザー入力に例外処理を用いることで、不正な入力や負の値を適切に弾く方法を学んだ。

# Day33

今日はグローバル変数とローカル変数の違いについて学んだ。
関数内で global を宣言すると、その変数は関数全体でグローバルとして扱われることを理解した。
また、代入は変数自体を新しく定義するのに対し、リストの要素変更はオブジェクトの中身を書き換えるだけなので global が不要であることも学んだ。
代入とミュータブルなオブジェクトの操作の違いを意識することが重要だと感じた。

# Day34

今日は、教材の9章続きから10章の途中までを学習した。

全体としては関数とturtleの基本的な使い方を学習した。まず、関数を引数として渡す処理を学び、関数自体を変数のように扱って実行できることを理解した。また、デフォルト引数についても確認し、引数を省略した場合の挙動を確認した。

その後、turtleを使って図形を描く処理を行った。for文を使って正多角形を描く方法を理解し、外角が「360 ÷ 辺の数」で求まることを学んだ。さらに、回転角度を「720 ÷ n」にすることで星型が描けることも確認した。

また、done()を実行するとturtleが終了状態になり、その後に描画命令を実行するとエラーになることを理解した。実行順序やプログラムの状態が結果に影響することを意識する必要があると感じた。

# Day35

今日はturtleモジュールを使って図形描画の練習を行った。正多角形や星型の図形を、回転角度（360/nや720/nなど）を調整することで描けることを理解した。また、複数のタートルを生成して同時に動かすことで、それぞれ異なる動きをさせられることも学んだ。

さらに、onscreenclick()を使ってマウスクリックに反応する処理を実装し、クリック位置と現在位置の中間に移動するロジックを扱った。座標や位置取得（pos()）の仕組みについても理解が深まった。

加えて、math.atan2()を用いて座標から角度を求める方法を学び、ベクトルと角度の関係についても触れた。最後に、座標の集合から独自の図形（ウサギ型）を定義し、カスタムシェイプとして登録する方法を習得した。

# Day36

今日は、turtleを使ってオリジナルの図形（ウサギ）を作成した。
座標データをタプルのリストとして定義し、register_shape()で独自の形を登録できることを学んだ。
また、shape()で形を指定し、stamp()で画面に描画する流れを理解した。

# Day37

今日はtkinterを使ったGUIアプリの基本構造とイベント処理について学習した。ボタンを押すことで関数が呼び出される仕組み（コールバック）を実装し、曜日を前後に切り替えて表示する処理を作成した。

特に、フレームワーク側がクリックイベントを検知し、登録した関数を自動で実行する「イベント駆動」の流れを理解できた。また、表示の更新はイベントの戻りではなく、自分のコードから新たに命令を出してUIを変更している点も重要なポイントだった。

tkinterは単なるUI作成ツールではなく、イベント管理も行うフレームワーク的な役割を持ち、さらに内部ではTcl/Tkを通してOSと連携している構造になっていることも理解できた。

# Day38

今日はtkinterを使ったGUIアプリの基本構造とイベント処理について学習した。ボタンを押すことで関数が呼び出される仕組み（コールバック）を実装し、曜日を前後に切り替えて表示する処理を作成した。

特に、フレームワーク側がクリックイベントを検知し、登録した関数を自動で実行する「イベント駆動」の流れを理解できた。また、表示の更新はイベントの戻りではなく、自分のコードから新たに命令を出してUIを変更している点も重要なポイントだった。

tkinterは単なるUI作成ツールではなく、イベント管理も行うフレームワーク的な役割を持ち、さらに内部ではTcl/Tkを通してOSと連携している構造になっていることも理解できた。

# Day39

本日は、Tkinterを用いた加算電卓プログラムのコードを細部まで読み込み、各処理の役割や動作の流れを理解した。特に、グローバル変数を用いた状態管理や、ボタン押下時のイベント処理の仕組みについて理解を深めることができた。

また、演習11-2としてGUIの見た目の改善を行った。具体的には、フォントを統一し、Frameやボタンの背景色を指定された色に変更したほか、ボタンのサイズを調整した。これにより、Tkinterにおけるウィジェットの外観設定方法について実践的に学ぶことができた。

今後は、機能の拡張（四則演算への対応など）や、より使いやすいUI設計についても取り組んでいきたい。

# Day40

本日は、足し算電卓を四則演算に対応させる演習に取り組んだ。ボタンの配置を工夫しながら、加算に加えて減算・乗算・除算の機能を実装した。また、割り算においては0で割る場合のエラー処理を考慮し、エラー表示を行うようにした。さらに、除算結果については整数商を用いる仕様に従い、「//」演算子を使用して実装した。

実装にあたっては、まず自分でコードを書き、実際に様々な操作を試しながら動作確認を行った。その過程で、エラー発生後に数値入力を行うと型の不一致による例外が発生する問題に気づき、自分で原因を特定し修正することができた。

また、すべてではないものの、想定されるエラーや不具合についても意識し、最低限の対策を講じた。今回の演習を通して、単に機能を追加するだけでなく、異常系の処理やプログラムの状態管理の重要性について理解を深めることができた。

今後は、より多くのケースに対応できるよう、エラー処理や設計の改善にも取り組んでいきたい。

# Day41

今日は京都大学プログラミング演習2023の第11章の終わりまで学習を進めた。

第11章では、tkinterを用いたGUIアプリケーションの作成と、画像の扱いについて学習した。特にPhotoImageクラスを用いた画像の読み込みと表示、そしてLabelウィジェットのimage属性を変更することで表示内容を切り替える仕組みを理解した。

演習ではトランプ画像を扱うプログラムを作成し、Cardsフォルダを用意して画像データを配置した。当初は画像ファイル名を手動でリストに記述していたが、osモジュールのlistdir関数を用いることでフォルダ内の画像ファイルを自動的に取得できるように改善した。この変更により、ファイル数が多い場合でも効率的に対応できるようになった。

また、file.endswith(".png")を用いてPNGファイルのみを抽出する処理を実装し、文字列オブジェクトのメソッドの使い方について理解を深めた。加えて、リストオブジェクトのappendメソッドによる要素追加や、sortメソッドによる順序の安定化についても確認した。

さらに、Pythonにおけるモジュール（osなど）とオブジェクト（文字列・リスト）との関係、メソッドと関数の違いについて整理することができた。特に「型によって使用できるメソッドが決まる」という点は重要な理解である。

加えて、Gitの運用として.gitignoreを作成し、Cardsフォルダを管理対象から除外した。これにより不要な画像ファイルをコミットせずに済み、リポジトリを適切に管理する方法についても学ぶことができた。

本日の学習を通して、GUI操作・ファイル操作・オブジェクト指向の基礎・Git管理といった複数の要素が相互に関連していることを理解できた。

# Day42

今日から「京都大学プログラミング演習2023」の第12章に入り、Tkinterを用いたGUIプログラミングとして、アナログ時計（プログラム12-1）の途中までを学習した。

今回の学習では、まずGUIの基本構造として、Tkinterにおけるウィンドウ生成（Tkクラス）や、ウィジェットという概念について理解を深めた。ウィジェットはボタンやラベル、キャンバスなどの画面上の部品であり、これらを組み合わせることでGUIが構成されることを確認した。

また、Canvasウィジェットを用いることで、図形（円や線）を描画できることを学び、これを使って時計の外枠や針を表現できる仕組みを理解した。加えて、packによるレイアウト管理や、width・height・bgといった基本的なプロパティについても把握した。

さらに、mathモジュールやtimeモジュールといった標準ライブラリの役割についても整理し、それぞれが三角関数計算や現在時刻の取得に使われることを確認した。特に、時間の値をもとにsinやcosを使って針の位置を計算するという考え方は、これまでのCUIプログラムとは異なる視点であり、新しい理解が得られた。

加えて、クラスとインスタンスの関係や、メソッド（例：title）の呼び出しといったオブジェクト指向の基本についても、実際のコードを通して具体的に捉えることができた。

今回の内容はまだ途中段階であるが、GUIプログラミングの基礎構造と描画の仕組みについて重要な理解を得ることができた。今後は、時計の針の動きや更新処理（afterメソッド）についても理解を深め、より完成度の高いプログラムに仕上げていきたい。

# Day43

12章途中のアナログ時計のコードについて、特に一部の処理に注目して理解を深めた。これまでなんとなく動きを追っていただけだったが、実際にどの処理がどの役割を持っているのかを意識して読み直すことで、コードの構造がよりはっきり見えるようになった。

昨日書いたコードの中でも、一部分を切り出して詳しく確認することで、全体の理解も自然と深まった。今後は一気に全体を見るのではなく、こうした部分ごとの理解を積み重ねていくことが重要だと感じた。

# Day44

今日はTkinterを使ったアナログ時計の基礎部分を理解した。
ウィンドウ作成（Tk）、タイトル設定、Canvasの作成と配置の流れを整理できた。
また、時計描画に必要な中心座標(cx, cy)と半径(r)の役割を理解した。

# Day45

角度のラジアン変換について理解と復習を行った。度数法と弧度法の関係を確認し、180度＝πラジアンという基準をもとに各角度を変換できることを再認識した。特に、三角関数で扱う際にはラジアンが前提になるため、スムーズに変換できることの重要性を理解した。今後は計算の中で自然にラジアンを扱えるように意識していきたい。

# Day46

今日も「京都大学プログラミング演習2023」の第12章の内容を進め、Tkinterによるアナログ時計（プログラム12-1）のコードの後半部分まで理解を深めた。また、文字盤（1〜12）の描画処理も追加し、時計としての完成度を高めた。
今回の学習では、特に三角関数（sin, cos）を用いた座標計算の仕組みについて理解が進んだ。中心座標から角度と半径をもとに円周上の点を求め、その点に向かって線を引くことで針を表現していることが分かった。さらに、Tkinterの座標系ではy軸が下方向に増加するため、cosの符号が反転している点も重要なポイントとして理解した。
また、秒針・分針・時針の長さや太さを調整することで視覚的な違いを表現していることや、時針が分の進行に応じて少しずつ動くように設計されている点についても納得できた。特に、1時間あたり30度、1分あたり0.5度という計算に基づいて角度を補正している部分は、プログラムと数学の結びつきを感じる内容だった。
さらに、afterメソッドによって一定時間ごとに関数を再実行する仕組みを理解し、GUIにおける更新処理の基本的な考え方を学んだ。これは通常のループとは異なり、イベント駆動型のプログラムの特徴を表していると感じた。
加えて、文字盤の数字を配置する処理では、360度を12分割して各位置にテキストを配置することで、円周上に均等に数字を並べる方法を学んだ。この部分でも三角関数を応用しており、同じ考え方で異なる要素（針と数字）を表現できる点が印象的だった。
今回の学習を通して、Tkinterの基本的な描画処理だけでなく、時間・角度・座標といった概念がどのように結びついて動的な表現を実現しているのかを理解することができた。今後は、目盛りの追加や針の動きの滑らかさの改善などにも取り組み、より完成度の高いGUIプログラムを目指していきたい。

# Day47

今日はPythonにおけるクラスの基本について学習した。
具体的には、クラス変数とインスタンス変数の違い、そしてインスタンス生成時に呼び出される__init__メソッドの役割を確認した。
クラス内で変数を定義することで全インスタンスで共有されるクラス変数を扱い、selfを用いて各インスタンス固有のデータ（インスタンス変数）を管理する構造を実装した。また、インスタンス生成時に通し番号を付与する仕組みを考え、クラス変数を更新することで状態を保持する方法を試した。
さらに、メソッドを通してインスタンス内部のデータにアクセスする方法や、複数のインスタンス間でのデータの独立性についても確認した。インスタンスごとにmydataの値が独立して変化することを実行結果から理解できた。
今回の演習を通して、オブジェクト指向の基本である「クラス」「インスタンス」「状態の管理」のイメージを掴むことができた。

# Day48

今日は前日に学習した内容をもう一度見直して、理解を深めることを中心に取り組んだ。
実際にコードを動かしながら確認することで、曖昧だった部分や流れがはっきりしてきて、前よりも納得しながら理解できるようになったと感じた。
また新しくファイル入出力について学び、プログラムの外にあるデータを扱えるようになることでできることの幅が広がると感じた。
ファイルパスについても学び、相対パスと絶対パスの違いや、実行場所によって挙動が変わる点などが重要だと分かった。
まだ慣れていない部分もあるので、実際に使いながら理解を定着させていきたい。

# Day49

昨日は、Pythonでのファイル入出力とワーキングディレクトリの扱いについて学習した。まず、osモジュールを使って現在の作業ディレクトリを取得する方法を理解し、プログラムがどの場所を基準にファイル操作を行っているのかを確認できるようになった。

その上で、open関数を用いたファイルの作成・書き込み・読み込みの基本的な流れを実際にコードで試した。書き込みモードと読み込みモードの違いや、ファイルを開いた後にcloseする必要があることも確認した。また、日本語を含むテキストを扱う際の挙動についても実際に動かして確かめることができた。

全体として、ファイル操作の一連の流れを理解できたことで、今後のプログラムでデータを保存・読み込みする基礎が身についたと感じた。

# Day50

今日はファイル操作の基本として、Pythonのopen関数の使い方を学んだ。特にファイル名とモードを指定してファイルを開く方法に加えて、encoding="utf-8"を指定することで文字コードを明示できることを理解した。日本語を扱う場合にエンコーディング指定が重要になる点も確認できた。これまでなんとなく使っていた部分だったが、引数の意味を意識して書けるようになったことで、エラーの原因も特定しやすくなると感じた。今後は読み込みや書き込み処理と組み合わせて、より実用的なファイル操作に発展させていきたい。

# Day51

今日は、Pythonでファイル操作を行うコードの確認をしました。
今回の学習を通して、ファイル操作を行う前に `open()` を使ってファイルを開き、変数へ代入する必要があることを再確認できました。また、エラーメッセージを読むことで原因を特定しやすくなることも学べました。
今後は `with open(...) as file:` の書き方や、読み込み・書き込み・追記モードの違いについても理解を深めていきます。

# Day52

本日は、Pythonでファイルを読み込む基本構文について学習しました。open()関数を使ってファイルを開き、for line in file:で1行ずつ処理する流れを確認しました。作業中に、文字列のクォーテーション不足やファイル名の指定方法など、構文エラーが発生しやすいポイントも理解できました。今後は、正しい記述に修正しながら、テキストファイルの読み込みと内容表示まで実装できるよう練習していきます。

